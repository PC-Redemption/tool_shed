# Codex App Server 0.153.0 v0.50.3 Work5 Evidence

Status: in-progress
Recorded: 2026-09-07
Release lane: `camp0171-v0.50.3`

## Product decision

Codex CLI 0.153.0 is an exact reviewed `unqualified` App Server version. Linux passed the supported
read-only and bounded workspace-write roles. Windows passed read-only operation but could not read
or mutate a fresh disposable workspace under either the legacy `workspaceWrite` policy or the
allowed `:workspace` profile. Tool Shed therefore routes this exact version back to the GUI until
the Windows boundary is repaired and requalified.

The cancellation probe now waits for the matching `turn/started` notification before interrupting,
closing the observed `no_active_turn` race while retaining bounded terminal-state reconciliation.

## Qualification

- Exact Linux `@openai/codex@0.153.0` executable SHA-256:
  `fce635028842bfe9257140e8b7d53162732945e2f356fc35225be0702b4974be`.
- Exact Windows 0.153.0 extension executable SHA-256:
  `0f8ed9678bca539aa6517adb0c8d50ad9a94ff5df4d21e149ae700d219fff69d`.
- The local release profile passed all 624 tests plus adapter, lifecycle-view, state, closure,
  manifest, and clean-workspace checks.
- Linux and Windows each passed the same 83 focused App Server, resolver, and reporting tests from
  candidate `baac68f1c914584cc806d7b8d310ccc0074d850f`.

## Development lanes

- Web: exact candidate `baac68f1c914584cc806d7b8d310ccc0074d850f` is healthy as
  `tool-shed-dashboard:dev-baac68f1c914` at image ID
  `sha256:211195346e5727e171a18c441db3cf8bf7acc2982a12163073adb284985232f2`.
  Both development health endpoints and the isolated production health check returned HTTP 200.
- Linux: disconnected candidate archive
  `sha256:95b0b4bfbf701ed7625847eb9f5ff08cead2e7050ee68c70d539912a61a4eb51`
  passed strict snapshot integrity and all 83 focused tests in
  `/home/jon/dev/ts_linux_test_bed`. Doctor verified internal consistency and reported only the
  pre-existing generated-index drift, which was preserved exactly.
- Windows: the identical archive passed strict snapshot integrity and all 83 focused tests in
  `GOGETTER:E:\dev\ts_windows_test_bed`. Doctor verified internal consistency and reported only
  the same pre-existing generated-index drift; the parent repository retained only those two
  modified index files.

## Release and production

- Frozen content commit and exact-SHA CI: pending.
- GitHub Release and all three production lanes: pending.
- Immediate dashboard convergence and owning outcome reconciliation: pending.
